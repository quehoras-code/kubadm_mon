global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: [
        {% for host in node_exporter_targets %}
          '{{ host }}:9100'{% if not loop.last %},{% endif %}
        {% endfor %}
        ]